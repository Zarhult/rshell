#ifndef __NIGHTMARE_HPP__
#define __NIGHTMARE_HPP__

#include <string>
#include <vector>
#include "base.hpp"

int findCloseParentheses(std::vector<std::string> &inputs, int start);

Base* nightmare(std::vector<std::string> &inputs);

#endif // __NIGHTMARE_HPP__
